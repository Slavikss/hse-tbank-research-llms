env_id: CartPole-v1
seed: 0
device: cpu
output_dir: artifacts/bc

expert_dataset_episodes: 20
expert_min_reward: 10.0

hidden_sizes: [32, 32]
lr: 0.001
weight_decay: 0.0
batch_size: 128
epochs: 10
val_ratio: 0.2
early_stop_patience: 4

bc_eval_episodes: 8

coverage_fractions: [0.3, 1.0]
shift_perturbation_stds: [0.0, 0.03]
error_compounding_episodes: 8
error_compounding_max_steps: 200
